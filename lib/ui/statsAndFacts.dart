import 'package:flutter/material.dart';

class StatsAndFactsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Stats & Facts'),
      ),
      body: Center(
        child: Text('Stats & Facts Page'),
      ),
    );
  }
}
